{% extends 'base.html.twig' %}

{% block title %}Réinitiliser votre mot de passe{% endblock %}

{% block main %}
    <h1>Réinitiliser mot de passe</h1>

    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Réinitiliser mot de passe</button>
    {{ form_end(resetForm) }}
{% endblock %}
