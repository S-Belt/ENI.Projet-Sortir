{% extends 'base.html.twig' %}

{% block title %}
    Ajouter un lieu | {{ parent() }}
{% endblock %}

{% block main %}
    {{ form_start(lieuForm) }}
    {{ form_widget(lieuForm) }}
    <button>Envoyer</button>
    {{ form_end(lieuForm) }}
{% endblock %}

